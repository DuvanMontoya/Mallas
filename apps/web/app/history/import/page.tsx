import { ModuleLanding } from "@/components/module-landing";

export default function ImportHistoryPage() {
  return <ModuleLanding eyebrow="Importación" title="Revisa antes de confirmar" description="Los archivos se validan, reconcilian y sólo afectan la auditoría después de una confirmación explícita." />;
}
