import { ModuleLanding } from "@/components/module-landing";

export default function GraphPage() {
  return <ModuleLanding eyebrow="Dependencias" title="Entiende qué desbloquea cada curso" description="El grafo muestra prerrequisitos, condiciones compuestas y caminos explicables." />;
}
