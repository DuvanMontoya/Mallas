# TEMPLATE ONLY — P24 must create the production Dockerfile after versions are resolved.
# Requirements: non-root runtime, multi-stage where useful, deterministic lockfile install,
# healthcheck/readiness handled by platform, no secrets baked into image.
